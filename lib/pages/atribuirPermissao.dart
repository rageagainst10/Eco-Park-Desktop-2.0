import "package:flutter/material.dart";

class AtribuirPermissao extends StatelessWidget {
  const AtribuirPermissao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
